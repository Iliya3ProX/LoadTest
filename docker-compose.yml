services:
  balance:
    build:
      context: .
      dockerfile: Balance/Dockerfile
    ports:
      - "5001:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
    networks:
      - loadtest-network

networks:
  loadtest-network:
    driver: bridge
